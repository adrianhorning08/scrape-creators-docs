const ScrapeCreatorsIcon = ({ className = "w-auto h-7" }) => {
  return (
    <img
      className={`relative object-contain ${className}`}
      src="https://scrapecreators.com/logo.png"
      alt="Scrape Creators logo"
    />
  );
};

export default ScrapeCreatorsIcon;
